{
  "name": "inference-sim",
  "private": true,
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "sim": "pnpm --filter @inference-sim/cli exec sim"
  }
}
